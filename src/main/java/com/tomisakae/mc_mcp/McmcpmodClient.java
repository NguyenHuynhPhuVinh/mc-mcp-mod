package com.tomisakae.mc_mcp;

import net.fabricmc.api.ClientModInitializer;

public class McmcpmodClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // Code dành riêng cho client (ví dụ: đăng ký renderer) sẽ ở đây
    }
}
